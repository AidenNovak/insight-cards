---
name: insight-cards
description: Generate insight cards — a dark, atmospheric artwork with a judgment-sentence insight typeset beneath it — entirely in code, with no image-generation model. Use when the user asks for an insight card, a "洞察卡片" / 判断卡片 / 分享卡片, a quote card, a dark poster with artwork and text, or wants to turn notes, quotes, or conclusions into shareable images in a consistent visual style. Also use when the user wants to add a new theme or palette to that card system, or asks what themes exist. Covers theme authoring, palette design, deck rendering, and format presets for social media.
---

# Insight Cards

把一条判断渲染成一张卡片：**上面是算出来的画面，下面是排出来的字。**

整条链路没有生图模型。这不是将就，是这套东西的前提：

- **算出来的图是程序，改一个参数就能看它怎么变；生出来的图是一个既定事实，改不动。**
- 判断句必须精确到字。交给图像模型，它会写错别字、会加装饰、会被光泽带走。
- 同一个判断要能换十几套配色、十几个意象重新出片，而版式一丝不变。

**图归算法，字归排版。** 只有这一条守住，上面三件事才成立。

## When To Use

- 用户要「洞察卡片 / 判断卡片 / 分享卡片 / 金句卡 / 深色海报 + 文案」
- 把笔记、语录、结论、复盘变成一张可以发出去的图
- 要给这套卡片**加新主题、新配色**，或者问「现在有哪些主题」
- 要一整套统一风格的卡片（deck），而不是单张

不适合：要真实照片写实效果（那该用生图）、要可编辑的矢量文件（这里输出 PNG）。

## Quick Start

```bash
cd <this skill's directory>          # 或者任何目录，用绝对路径调 bin/
node bin/make-card.mjs --themes      # 看有哪些主题（18 个）
node bin/make-card.mjs --palettes    # 看有哪些配色（18 组）

node bin/make-card.mjs \
  --theme starfield --palette deep-field \
  --title "你不是在收集碎片，你在连一个只有你能看见的形状" \
  --summary "几百条记录躺在那里的时候什么都不是。是你反复看的那几条，把它们连成了一个形状——而那个形状别人看不见，因为它只存在于你的注视里。" \
  --out out/card.png
```

默认输出 1080×1440 @2x（2160×2880）。渲染约 1–3 秒。

## 怎么选主题与配色

**这一组东西的质量取决于「意象」与「文案」是否真的对得上。**
随便挑一个主题套一句文案，出来的是装饰；对上了才是表达。

主题的 `meta` 里有四个字段帮你判断，用 `--themes` 看：

| 字段 | 含义 |
|---|---|
| `connects` | 它表达「连接」的哪一种机制（见下表） |
| `mood` | 它让读者处于什么情绪里 |
| `zh` `en` | 名字，会出现在卡片页脚 |

现有十八种机制，**新主题的 `connects` 必须不同于其中任何一个**：

<!-- gen:tables:start -->
| 机制 | 主题 | 机制 | 主题 |
|---|---|---|---|
| 约束 | 光纤 | 磁场 | 极光 |
| 共振 | 共鸣 | 折射 | 棱镜 |
| 共生 | 双星 | 扩散 | 水中墨 |
| 延迟 | 回响 | 堆积 | 沉积 |
| 击穿 | 放电 | 周期 | 潮汐 |
| 引力 | 星云 | 累积 | 潮痕 |
| 轴心 | 星轨 | 激发 | 突触 |
| 注视 | 星野 | 交换 | 菌丝 |
| 结晶 | 晶格 | 同步 | 萤火 |
<!-- gen:tables:end -->

**配对的方法**：先读文案在说什么，再问「这件事**怎么**发生的」。

- 文案讲「我一直没注意到的那条线」→ 机制是**注视** → 星野
- 文案讲「散着的时候什么都不是，聚起来才有形状」→ 机制是**引力** → 星云
- 文案讲「量变到某个点突然通了」→ 机制是**击穿** → 放电
- 文案讲「退潮时才看得见，涨潮时又没了」→ 机制是**周期** → 潮汐
- 文案讲「每一年都很薄，是它们摞起来才有分量」→ 机制是**堆积** → 沉积
- 文案讲「很久之后它以衰减的形式回来」→ 机制是**延迟** → 回响
- 文案讲「两个各自在动，相遇处特别强」→ 机制是**共振** → 共鸣
- 文案讲「成分没变，变的是它们的排列」→ 机制是**结晶** → 晶格
- 文案讲「一直是那个东西，是某次转折才看出里面有什么」→ 机制是**折射** → 棱镜
- 文案讲「谁也没绕着谁，我们绕着彼此之间那个空的地方」→ 机制是**共生** → 双星
- 文案讲「每次都很浅，是来了很多次才刻出这道沟」→ 机制是**累积** → 潮痕

配色同理，但选的是**温度**：同一句判断放在「深空」里是冷静观察，放在「余烬」里是警告，
放在「琥珀」里是隔着一层时间的怀旧。配色表里的 `mood` 写的就是这件事。

> 一个常见错误：为了好看挑配色。**配色是语气，不是装饰。**
> 如果一句自省的话配了「余烬」，读者会读成指责。

## 两套版式

| `--layout` | 用途 | 特征 |
|---|---|---|
| `plate`（默认） | 阅读、公众号、文档 | 图在上，底部一段长渐变化进底色，字落在底色上 |
| `poster` | 分享、社交媒体、壁纸 | 整张满幅，字压在图上，靠长遮罩拿到可读性 |

`poster` 更冲，适合一眼；`plate` 更稳，适合被人认真读。
判断标准：**这张卡片是要被划过去的，还是要被看完的。**

## 其它参数

```
--index 03 --total 12     卡片序号（页眉显示 "03 / 12"）
--scale 1|2|3             输出倍率（默认 2；1x = 1080×1440）
--kicker "洞察"           页眉右侧的短标签（默认空）
--footer "..."            页脚右侧（默认用配色的情绪词，即这张卡的 mood）
```

`--footer` 有一个用法值得知道：**它可以放日期或来源**，让卡片带上时间坐标 ——
「2026 年 3 月」比任何装饰都更能说明这是什么时候的想法。

**关于序号：`--index` / `--total` 只在单张渲染时要你自己给。**
`make-deck.mjs` 会自动编（按预设里的顺序填 `01`、`02`…），你不用管。
所以做一套卡的正确方式是：

- 在 `packages/engine/presets.mjs` 里把这一套排成一组 `cards`，用 `make-deck.mjs` 出图 ——
  序号、总张数、文件名的编号前缀都会自动对上；
- 只有临时试一张、或补做某一张时，才用 `make-card.mjs`，
  这时**记得手给 `--index` / `--total`**，否则页眉会显示 `01 / 1`，
  放进一套卡里就露馅了。

一次试用在这里卡过：文档只列了参数名，没说"什么时候要自己给"，
于是单独出的卡和整批出的卡序号规则不一致。

---

## 加一个新主题

主题是一个 `.mjs` 文件，放在 `packages/engine/themes/`。
**写之前先读 `references/THEME-SPEC.md`** —— 接口契约、六个技法、自检清单都在里面。
**动手前先读 `references/DESIGN.md`** —— 特别是第一节"图和字必须是同一件事说两遍"，
它决定了这个主题该不该存在。

最短的骨架：

```js
export const meta = {
  id: "tide", zh: "潮汐", en: "Tide",
  connects: "周期",
  mood: "被一个更大的东西定期带着走",
  note: "为什么这个意象表达了连接",
};

export function render(ctx, { w, h, rng, palette }) {
  ctx.fillStyle = rgb(palette.base);
  ctx.fillRect(0, 0, w, h);
  // ... 主体
  vignette(ctx, w, h, 0.48, 1.28);
  grain(ctx, w, h, rng, 0.045, 2);
}
```

三条硬约束（违反就不是模板，只是一张图）：

1. **不许写死颜色。** 一切从 `palette` 派生。要白色就
   `mix(palette.glow, [255,255,255], 0.8)`。
2. **不许用 `Math.random()`。** 必须用 `rng`，否则同一输入每次渲染都不一样。
3. **不许 import。** kit 与 rng 的导出会被注入作用域。

写完必须自己渲染出来看一眼，逐条过 `references/THEME-SPEC.md` 的自检清单。
最容易忽略的是最后一条：**光必须照亮周围** —— 一个发光点如果周围没被它照亮，
眼睛立刻知道是假的。

### 写主题时最容易踩的坑

这些都是在实际写这些主题时踩过的，按踩到的频率排：
（数量不写死 —— 写了就会过期，实际数字看 `--themes` 的输出。）

| 现象 | 真正的原因 |
|---|---|
| 画面像**图表/示意图**（靶心、条形码、等高线） | 画了**等距、均匀、完整闭合**的几何。真实的东西一定是不等距、有衰减、有缺口的 |
| 画面**糊成一片**，看不出结构 | 逐像素渲染的场太软。**改用线** —— 结构靠线与线的关系被认出来，不靠亮度分布 |
| 画面像**贴纸**，光浮在背景上 | 缺环境提亮（DESIGN.md 第 2.2 条）：发光体周围要有一层大而淡的提亮 |
| 画面**死黑**，什么都看不见 | 把"亮"当成了"压暗"的结果。应该先给材质上颜色，再让光决定哪里沉下去 |
| 画面像**丝绸/金属拉丝**，太光滑 | 缺微观质感。加不规则的低对比斑点，把"光滑"打碎 |
| 主体读错了（比如把墨滴画成了眼睛） | 某个**图形特征**触发了别的联想（硬边 + 闭合的亮环 = 眼睛）。要改掉那个特征，不是调参数 |
| 某个元素**抢戏**了 | 有多于一个"最亮的东西"。主光只能有一个，其余必须明显更弱更小 |
| 画成了一排**波形 / 靶心 / 条形码** | 等距、均匀、完整闭合 = 图表。**换图形，别调参数** —— 那是那个图形的定义式 |
| 点阵看起来像**坐标纸 / 屏幕像素** | 缺"多晶"结构。真实物质由多个取向不同的晶粒拼成，粒与粒交界处才是关键 |
| **我画了但看不见** | `lighter` 合成里深色 + 小 alpha ≈ 0。颜色至少取到 `stops[2]` |
| 一条曲线上出现**一串等距亮点** | 分段调 alpha 描边，`lineCap:"round"` 的圆头在接缝处叠亮了。改成拆成几层**连续**折线 |

## 加一个新配色

改 `packages/engine/palettes.mjs`，往 `PALETTES` 里加一项，然后跑
`node bin/palette-check.mjs` 确认它和已有的组不会分不出来。字段含义见文件头注释。

**补配色时先问：还有哪些情绪没有被任何一组表达过？**
"再挑几个好看的色"会得到一组彼此雷同的配色 —— 体检工具会告诉你哪些太近。

两条经验：

- `base` 必须是**近黑但有倾向**的颜色。纯黑在屏幕上发死，偏一点蓝、绿或棕才像"夜"。
- `stops` 色阶的**最后一档要接近白**，否则光立不起来；**第一档要够暗**，
  否则暗部没有层次。中间两档决定这张卡的"温度"。

## 渲染一整套

```bash
node bin/make-deck.mjs --preset nexus --out out/nexus
node bin/make-deck.mjs --preset undertow --out out/undertow
node bin/make-deck.mjs --preset structure --out out/structure
```

预设定义在 `packages/engine/presets.mjs`，包含一整套「主题 × 配色 × 文案」。
新增预设照抄现有结构即可。

**一套 deck 的编排原则**：不要全部用同一个配色。同一批卡片里，
配色负责给每张定温度，主题负责给每张定机制 —— 两者都重复会让整套看起来是同一张。

---

## 校验

**改完一定要跑下面这些，看图之前先跑。**

```bash
node bin/audit.mjs            # 确定性 + 配色响应 + 暗部占比（每个主题）
node bin/grid.mjs             # 全网格：18 主题 × 18 配色 × 3 文案长度
node bin/readability.mjs      # 文字可读性：量文字底下有多亮（WCAG 对比度）
node bin/doc-check.mjs        # 文档与代码是否一致（参数名、命令名、声明的数量、机制表）
node bin/gen-tables.mjs       # 从主题文件重新生成机制表（加了新主题就要跑）
node bin/palette-check.mjs    # 配色之间是否真的可分（排 deck 之前跑，别用两组相近的）
```

`audit` 报四件事：每个主题是否**确定性**（两次渲染是否一致）、
是否**响应配色**（换配色画面要变色，整体色距与亮部色相各问一次）、
**暗部占比**是否够暗、以及**几何是否独立于配色**
（配色只决定颜色，不决定形状 —— 这一条是契约，有静态检查守着）。

**"缩到 200px 仍认得出主体"不在这四件事里，它是人工自检项。**
原因是它没有能自校准的读数：曾经写过一个形态度量想自动化这一条，
实测分不开"两个不同的主题"与"同一主题换个种子"（跨主题最小 0.721，
低于自身变异中位 0.771）—— 那种指标上线等于发布一个假检查，已删。
所以这条留在 `references/THEME-SPEC.md` 的自检清单里，靠眼睛过。

**改判据必须带正控。** 上面"亮部色相"这一条的第一版是逐像素比色
（问"有多少亮像素换配色后一点没动"），在 fiber 上用两种写法注入硬编码，
它都报 0%、全绿 —— 因为这套引擎的画几乎都走 alpha 合成，
像素 = 硬编码色 × α + 配色底 × (1−α)，配色底一变合成色就跟着变。
现在的版本量的是**整体色相移动**，标定过：18 个正常主题 151°–167°，
写死主体后掉到 41° / 8.6°，阈值取 100°。
教训是：一条新判据要先证明它**会失败**，再相信它会通过。

`palette-check` 另有一件容易忽略的事：**配色不能只是"不同"，还要"够不同"。**
它会报出任意两组配色之间的色距，低于阈值的就是用户分不出来的一对。

同一套 deck 内部也守同一条线：**一套卡里不能有两组读者分不出来的配色**，
否则那套卡看起来像同一张印了很多遍。这条由 `doc-check.mjs` 检查预设时一并查。

（这个限制只解决一半问题。**"这组配色配这句话合不合适"没有可查的判据** ——
它属于语气，语气有品味成分，工具管不了。一次试用提出过这一点，说得对：
主题层有"机制必须不重复"这样的硬规则，配色层只有"要配得上"这种说法。
能机械化的那一半（别撞车）已经加上了，剩下那一半只能靠人看。）

### 有一条核心规则没有任何工具守着，必须说清楚

这套东西的核心主张是「**画面必须是关于内容的**」——图和字是同一件事说两遍。
**而这是唯一一条完全没有工具守着的规则。** 数量有 `doc-check`、
确定性有 `audit`、配色有 `palette-check`，唯独"这个主题画的是不是它说的那个机制"
只有作者的自觉。

而且要小心一个误解：`doc-check` 会报「机制重复」，
**但它查的是 `connects` 这个字符串有没有重名，不是机制在语义上是否真不同。**
两个主题可以机制完全相同、只是换了个说法，检查照样全绿；
一个主题也可以画着别的东西而 `connects` 只是个贴上去的标签，检查同样全绿。
（核验期间就出现过这种情况：一个主题文件被换成了另一个主题的绘制代码、
只保留自己的 meta，`doc-check` 报的是「✓ 文档与代码一致」。）

这一条**不可能机械化**——"这句话的机制是不是这个形状"是语义判断，
没有可计算的判据。能做的是两件：写主题时先把 `references/DESIGN.md` 第一节
（"图和字必须是同一件事说两遍"）读一遍；以及**新主题一定要人看图**，
不能只靠检查通过就认为对了。

**耗时预期**（本机，18 主题 × 18 配色）：

| 检查 | 规模 | 耗时 |
|---|---|---|
| `audit` | 18 张 | 约 30 秒 |
| `grid` | 972 张 | 约 10 分钟 |
| `readability` | 648 张（每张渲染两次） | 约 10–15 分钟 |

超过 10 分钟的活建议丢给远程（见下）。**中途抛异常时先怀疑资源，不要先怀疑主题** ——
已经踩过一次：每张卡启停浏览器，一千多次之后进程被拖垮，
报出来的是渲染无关的错。现在全程复用一个浏览器。

想体检**你自己那句话**而不是内置文案：

```bash
node bin/readability.mjs --title "你的判断句" --summary "你的摘要" --layout poster
```
`grid` 会挑三种文案长度 —— **短句最容易暴露问题**（标题不满一行时版式容易失去重心），
长段落则考察溢出与折行。

**`readability.mjs` 为什么重要**：`poster` 版式的全部前提是"靠遮罩拿到可读性"，
在这之前没有任何东西验证过它。实测 `discharge` 的卡上闪电正好从标题后面穿过，
而遮罩是固定梯度，它不知道背后的画面有多亮。改用 DOM 取文字位置、
量它底下的背景亮度（WCAG 对比度：标题 ≥3.0，摘要 ≥4.5）之后，
遮罩被加重了一轮。**这个文件里我量错过三次**，都写在注释里了 —— 值得一读，
因为"量错了对象"是这类自动检查最容易犯的错。

看图用这三个：

```bash
node bin/gallery.mjs --out out/gallery.html                     # 整套画廊（可离线打开）
node bin/contact-sheet.mjs --preset nexus --out out/sheet.png   # 整套并排
node bin/palette-sheet.mjs --out out/palettes.png               # 全部配色并排
node Tools/thumb-sheet.mjs <主题...>                             # 缩略图尺度并排（见下）
```

**缩略图要用 `Tools/thumb-sheet.mjs` 真的缩出来看。** 把主题按真实配色渲染、
缩到 200px 宽（信息流里就是这个尺寸）、并排成一张图：

```bash
node Tools/thumb-sheet.mjs orbit synapse starfield --palette abyss
```

这条清单项没有自动判据（试过一个形态度量，分不开"不同主题"与"同主题换种子"，
见 `bin/audit.mjs` 顶部），只能人看 —— 但要看的是**真的 200px**，
不是全尺寸图在脑子里缩。实测踩过：`orbit` 的轨道线在 1.3 单位宽时
缩到 200px 只剩 0.24 像素，画面里只剩两个亮点，而它的机制全在那条线上。

**协调是整体的性质，只有并排才看得出来。** 单张一张张看，看不出哪几张暗了、
哪几张太满 —— 而"这一套不像一套"恰恰是这种工作最容易出的问题。

### 批量渲染放到服务器上

单张 1–3 秒，本地跑几百张会占满 CPU。用 `Tools/remote-render.sh`
（默认 `vultr-sg`，8 vCPU / 31 GiB）：

```bash
Tools/remote-render.sh check     # 环境自检
Tools/remote-render.sh grid      # 全网格，出问题清单
Tools/remote-render.sh themes    # 每个主题一张成品，取回 out/remote/
Tools/remote-render.sh clean     # 清掉服务器上的安装与产物
```

服务器上同时跑着生产容器，脚本已用 `nice`/`ionice` 让路。

## 故障排查

| 现象 | 原因与修法 |
|---|---|
| `找不到可用的 Playwright` | 报错里已给出修复命令，照抄执行 |
| 渲染**超时**，没有报错 | 主题里抛了异常，`__done` 永不置位。用 `node Tools/dbg-theme.mjs <主题> <配色>` 拿到真实报错 |
| 画面是一坨灰 | 主题没有明确的暗部与亮点。按 THEME-SPEC §三.4 用幂律重做强度分布 |
| 换配色画面不变 | 主题里写死了颜色，违反硬约束 1 |
| 两次渲染结果不同 | 漏用了 `rng`（`Math.random()` 或时间戳），违反硬约束 2 |
| 文字被页脚挤出去 | 渲染器会自动缩小标题（最多 6 级）。若仍溢出，说明摘要太长——缩短它，不要缩字号 |
| 中文字体不对 | 卡片走系统字体栈（PingFang SC / Noto Sans SC）。Linux 上需要装中文字体 |

## 文件地图

```
insight-studio/
├── SKILL.md                      ← 你在这里
├── references/
│   ├── DESIGN.md                 设计理念（写新主题前读，决定该不该做）
│   └── THEME-SPEC.md             主题编写规范（写新主题前必读）
├── packages/engine/
│   ├── render.mjs                渲染器：主题 + 文案 → PNG
│   ├── kit.mjs                   绘图原语（glow / ramp / offscreen / grain / ...）
│   ├── rng.mjs                   确定性随机 + Perlin/fBm/warp 噪声
│   ├── palettes.mjs              18 组配色
│   ├── presets.mjs               整套 deck 的编排
│   └── themes/*.mjs              18 个主题
├── bin/
│   ├── make-card.mjs             单张
│   ├── make-deck.mjs             整套
│   ├── audit.mjs                 确定性与配色响应
│   ├── grid.mjs                  全网格
│   ├── readability.mjs           文字可读性（WCAG）
│   ├── doc-check.mjs             文档与代码一致性
│   ├── gen-tables.mjs            从主题文件生成机制表
│   ├── palette-check.mjs         配色可分性
│   ├── contact-sheet.mjs         整套并排
│   ├── palette-sheet.mjs         配色并排
│   └── gallery.mjs               画廊 HTML
├── Tools/
│   ├── remote-render.sh          远程批量渲染
│   ├── thumb-sheet.mjs           缩略图（200px）并排，查缩略图生死线
│   └── dbg-theme.mjs             单主题调试
├── registry/                     已排好的 deck（成品图）
└── out/                          渲染产物
```

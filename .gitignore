# 渲染产物。每一次跑都是几百 MB，不进版本库。
out/

# registry/ 是**要进版本库的**：它是排好的成品 deck，
# 是这个项目最重要的交付物之一（别人可以直接看效果，不用自己跑一遍）。
# 所以这里不忽略它。

# trials/ 是**试用记录，不是渲染产物**，所以要进版本库。
#
# 这里踩过一次，而且是连着踩两次，值得写清楚：
# 一开始试用记录放在 out/notes-trial/，而 out/ 被忽略 —— 于是"留着下次参考"
# 的记录在提交时会被静默丢掉（说了要留，实际留不住）。
# 搬到 trials/ 之后，我在这份 .gitignore 里补了一句"trials/ 要进版本库"，
# **但同时写了 `trials/` 这一行 —— 那正好是忽略它的意思**，等于又丢一次。
#
# 教训：想"不忽略"，正确做法是**什么都不写**。
# 永久留一行注释在这里，防止下一个人再写一遍 `trials/`。
#
# 图片只留缩到 675px 的版本（约 2.4 MB），够看清画面，体积也可控。

# 依赖
node_modules/
package-lock.json

# 系统与编辑器
.DS_Store
*.swp
.idea/
.vscode/
